<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper	namespace="com.example.codeblue.board.mapper.QuestionBoardMapper">

	<select id="selectQuestionCommentList" parameterType="int" resultType="com.example.codeblue.board.vo.QuestionComment">
		SELECT
			question_comment_id as questionComment,
			user_id as userId,
			question_comment_content as questionCommentContent,
			question_id as questionId,
			question_comment_datetime as questionCommentDatetime
		FROM question_comment
		WHERE question_id = #{qestionId}
		ORDER BY question_comment_datetime DESC
	</select>
	
	<select id="selectQuestionCommentCount" resultType="int" parameterType="int">
		SELECT 
			COUNT(question_id) 
		FROM question_comment
			WHERE question_id = #{questionId}
	</select>
	
	<select id="selectQuestionBoardOne" parameterType="int" resultType="com.example.codeblue.board.vo.QuestionBoard">
		SELECT 
			question_id as questionId,
			question_title as questionTitle,
			question_content as questionContent,
			question_datetime as questionDatetime,
			feild_id as feildId,
			user_id as userId,
			tag_id as tagId
		FROM question_board
		WHERE question_id = #{questionId}
	</select>
	<select id="selectQuestionBoardVotes" resultType="int" parameterType="com.example.codeblue.board.vo.QuestionBoard">
		SELECT 
			COUNT(question_id) 
		FROM question_vote
			WHERE question_id = #{questionId}
	</select>
	
	<select id="selectQuestionBoardAnswerCount" resultType="int" parameterType="com.example.codeblue.board.vo.QuestionBoard">
		SELECT 
			COUNT(question_id) 
		FROM answer
			WHERE question_id = #{questionId}
	</select>
	
	<select id="selectQuestionBoardList" resultType="com.example.codeblue.board.vo.QuestionBoard" parameterType="com.example.codeblue.board.vo.Page">
		SELECT 
			question_id as questionId,
			question_title as questionTitle,
			user_id as userId,
			question_datetime as questionDatetime
		FROM question_board 
		<where>
			<if test="searchWord != null">
				question_title like CONCAT('%',#{searchWord},'%')
			</if>
		</where>
			ORDER BY question_datetime DESC 
			LIMIT #{beginRow},#{rowPerPage}
	</select>
	<select id="selectQuestionBoardTotalCount" resultType="int" parameterType="com.example.codeblue.board.vo.Page">
		SELECT 
			COUNT(*)
		FROM question_board
		<where>
			<if test="searchWord != null">
				question_title like CONCAT('%',#{searchWord},'%')
			</if>
		</where>
	</select>
</mapper>