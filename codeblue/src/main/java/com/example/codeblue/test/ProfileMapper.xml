<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.codeblue.test.ProfileMapper">
	<select id="selectUserOne" parameterType="com.example.codeblue.user.vo.User"
			resultType="com.example.codeblue.user.vo.User">
		SELECT 
			   user_id as userId,
			   user_name as userName,
			   region_id as regionId,
			   user_birthdate as userBirthDate,
			   user_createtime as userCreatetime
		FROM user
		WHERE user_id = ${userId}
	</select>

</mapper>